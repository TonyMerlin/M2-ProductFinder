var config = {
    map: {
        '*': {
            // Alias the legacy module ID to the core jQuery UI from lib/web
            'Magento_Ui/js/lib/jquery/jquery-ui': 'jquery/jquery-ui'
        }
    }
};
